package main

// "log"

func main() {
	// handlers := new(handle.Handler)
	// server := new(server.Server)
	// if error := server.Run("8080", handlers.InitRoutes()); error != nil {
	// 	log.Fatalf("Error occured while running http server: %s", error.Error())
	// }
}
